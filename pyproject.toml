[project]
name = "sambandha-platform"
version = "0.1.0"
description = "Quotation and project-intake workflow for Sambandha Interior & Exterior."
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
  "Django>=5.2.8,<5.3",
  "dj-database-url>=2.3,<4",
  "gunicorn>=23,<24",
  "psycopg[binary]>=3.2,<4",
  "python-dotenv>=1.0,<2",
  "whitenoise>=6.9,<7",
]

[tool.uv]
package = false
